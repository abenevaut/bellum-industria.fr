version: 2

# Pre composer execution
#pre: artisan bellumindustria:production-pre-deploy

# Composer settings
composer:
    prefer-source: false
    prefer-dist: true
    no-dev: true
    optimize-autoloader: true
    classmap-authoritative: true

# Post composer execution
post: artisan bellumindustria:production-post-deploy
