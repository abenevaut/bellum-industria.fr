Deploy:
  script:
  - chmod -R 777 storage
  - git submodule init
  - git submodule update
  - composer install --no-dev
  - npm install
  - gulp production
  - vendor/codeception/codeception/codecept run
